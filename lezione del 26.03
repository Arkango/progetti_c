Puntatori :
nome, tipo , locazione di memoria.

non abbiamo il completo controllo dei valori assumibili da un puntatore (dipende dai range di valori messi a disposizione dal os al loader del c) . Valori espressi in esadecimale .
---------------------------------------------------
si definisce con :

type *name_varibale ; //esempio
int *myPointer; //implementazione
--------------------------------------------------
attraverso l'operatore cast si forza una varibile ad assumere valori di un altro tipo.
i possibili valori sono 0, NULL, indirizzo di mem.

inizializzione : meglio inizializzarlo a NULL, per evitare incongruenze.

operatori

* puntatore
& referenziazione

scanf richiede come lista di parametri indirizzi

//guardare slide dei Puntatori

funzione astratta deve eseguire un'espressione aritmetica, prendersene il valore , senza modificare lo stato delle varibili;

non collide con il passaggio per riferimento


calcolo dim struct , multipli di 4 byte

un valore di un puntatore può essere modificato anche semplicemente rendendolo costante

puntatore ad array

int array[10]
int* p_array;

p_array = &array[0];
p_array = array;



puntatori a funzioni
